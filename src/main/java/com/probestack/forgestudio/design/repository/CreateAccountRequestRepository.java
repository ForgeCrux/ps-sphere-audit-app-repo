package com.probestack.forgestudio.design.repository;

import com.probestack.forgestudio.design.model.CreateAccountRequest;
import java.lang.Long;
import org.springframework.data.mongodb.repository.MongoRepository;

/**
 * MongoDB repository for CreateAccountRequest entities.
 *
 * <p>This repository extends MongoRepository which provides standard CRUD operations:</p>
 * <ul>
 * <li>save(entity) - Create or update an entity</li>
 * <li>findById(id) - Find entity by ID</li>
 * <li>findAll() - Retrieve all entities</li>
 * <li>existsById(id) - Check if entity exists</li>
 * <li>deleteById(id) - Delete entity by ID</li>
 * <li>count() - Count all entities</li>
 * </ul>
 *
 * @author Generated by ProbeStack CodeGen
 */
public interface CreateAccountRequestRepository extends MongoRepository<CreateAccountRequest, Long> {
}
